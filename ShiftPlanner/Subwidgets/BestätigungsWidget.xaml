<Window x:Class="ShiftPlanner.Subwidgets.BestätigungsWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShiftPlanner.Subwidgets"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Bestätigung" Height="300" Width="400
        ">
    <StackPanel>

        <TextBlock x:Name="Infotext"
                   Text="Hier könnte deine Info stehen"
                   Padding="10"
                   Margin="0,0,0,20"
                   TextWrapping="Wrap"/>

        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
          Content="Bestätigen"
          Width="80"
          HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Click="Bestaetigung_Click"/>

            <Button Grid.Column="1"
          Content="Ablehnen"
          Width="80"
          HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Click="Abbruch_Click"/>

       </Grid>

    </StackPanel>
</Window>
